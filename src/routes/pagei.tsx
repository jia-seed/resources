import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/pagei')({
  component: PageI,
})

function PageI() {
  return (
    <div style={{ padding: '0 4rem 4rem' }}> 
    
    </div>

  )
}
